runtime: nodejs
vm: true

service: default

health_check:
  enable_health_check: false

default_expiration: '4d 5h'

handlers:
- url: /.*
  script: IGNORED
  secure: always

env_variables:
  ENV: dev

skip_files:
  - ^(.*/)?.*/node_modules/.*$
  - ^(.*/)?.*/client/assets/.*$
  - ^(.*/)?.*/.git/.*$